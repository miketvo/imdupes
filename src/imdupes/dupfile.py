from utils.imutils import ImageFileWrapper


def load(
        file: str,
        exclude: str = None,
        verbose: int = 0
) -> list[list[ImageFileWrapper]]:
    dup_imgs = []
    return dup_imgs
